package config

// OPEN_ROUTER_KEY should be replaced with your actual key before building/installing, 
// or you can inject it during the build process.
const OPEN_ROUTER_KEY = "sk-or-v1-YOURKEY"

// MODEL_ID defines the specific OpenRouter model we want the AI terminal helper to use.
const MODEL_ID = "stepfun/step-3.5-flash:free"
